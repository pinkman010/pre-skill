---
name: pre-skill
description: 用于在制作 PPT 或使用任意 PPT 生成平台之前，先完成材料拆解、任务要求提取、叙事逻辑分析、逐页大纲、逐页可见文案、讲稿、图表使用说明和图像生成提示词，最终产出一份详细中文 PPT 文字底稿。适用于论文答辩 PPT、课程 presentation、课堂汇报、案例展示、读书报告、研究汇报、项目汇报和商业展示。默认不生成 .pptx；只有用户明确要求“根据底稿生成 PPT”时，才继续调用 presentations 等技能装配成文件。触发词包括 PPT前期准备、PPT文字底稿、pre底稿、课程presentation、答辩PPT、论文拆解、课程要求拆解、逐页大纲、先别做PPT、按流程做PPT。
---

# pre-skill

这个 skill 的核心任务是生成可直接用于 PPT 生成平台或人工排版的中文文字底稿。它先把材料、要求、叙事和素材关系拆清楚，再产出逐页可执行的内容蓝图。

默认交付物是 `07-final-draft/pre-draft.md`。`.pptx` 文件属于可选后续交付物。

## 支持模式

尽早选择一种模式：

- `thesis`：论文答辩、开题、中期、研究汇报、毕业设计展示
- `course`：课程 presentation、课堂汇报、案例分析、读书报告、小组展示
- `generic`：项目汇报、方案展示、研究分享、产品或商业汇报

模式只影响判断重点，工作流保持一致。

## 目标

把原始材料整理成一份满足演讲目标的 PPT 文字底稿，底稿应包含：

- 报题版：题目、100-150 字简介或课程提交摘要
- Presentation 统一视觉规范：主题风格、配色、字体、页面结构、元素使用
- 任务要求、评分标准、听众、时长和格式约束
- 原始材料拆分后的内容块
- 图表、截图、表格、图片和可用视觉素材索引
- 展示逻辑 memo：问题、主张、证据、取舍、追问风险
- 主讲页和备答页的逐页大纲
- 每页的页面标题、页面副标题、上屏内容、页面结论、展示建议、版式建议、讲解脚本和转场句
- Presentation 制作与展示建议：视觉呈现、标题与信息密度、时长控制
- 表达边界：事实边界、法律边界、结论边界、禁用或慎用表述
- 每页的图表使用方式、布局建议、生成图提示词
- 可投喂 PPT 生成平台的最终中文底稿

## 语言规则

默认所有面向用户的内容使用中文，除非用户明确要求其他语言。

适用范围：

- markdown 标题和表头
- 页面标题、页面可见文案、讲稿、转场句
- 展示逻辑分析和追问准备
- 图表说明、视觉规划和图像生成提示词说明
- 最终 PPT 文字底稿

文件名、脚本参数、JSON 字段名可以保留英文，以便自动化处理。

## 强制门禁

这些规则必须执行：

1. 未提取任务要求和可用素材前，不写逐页底稿。
2. 已有结构化内容块后，不反复啃重型 Word/PDF，除非任务重点是版式还原。
3. 未完成展示逻辑 memo 前，不写逐页大纲。
4. 未稳定逐页大纲前，不写逐页文案。
5. 默认不调用 `presentations`，也不直接生成 `.pptx`。
6. 只有用户明确要求“根据底稿生成 PPT”时，才进入 PPT 装配阶段。
7. 每个主讲页素材都必须说明它支撑的观点、要求或讲解点。
8. 每页必须说明页面目标、核心信息、素材位置、讲解重点、预计讲时。
9. 主讲内容、备答内容、生成图需求必须分开。
10. 生成图只能辅助理解、氛围或概念表达，不能替代实验图、真实截图、数据图表、课程指定材料或引用来源。
11. 涉及法律、伦理、医学、金融、政策或争议事实的展示，必须写表达边界，避免把“争议、指控、初步认定、个案判断”写成确定结论。

## 需要收集的输入

只有缺失信息会明显影响结果时才询问用户。

- 源文件：docx、pdf、pptx、md、txt、xlsx、图片
- 展示模式：`thesis`、`course` 或 `generic`
- 展示时长和语言
- 听众、课程要求、评分标准或答辩标准
- 学校模板、课程 rubric、品牌规范或格式限制
- 是否需要备答页、附录或 Q&A 准备
- 是否允许生成图像
- 是否需要报题版、课程提交摘要或小组汇报简介
- 最终底稿是否需要适配某个平台，例如 Gamma、Canva、Beautiful.ai、WPS AI、PowerPoint Copilot

## 技能串联

按需要调用其他技能：

- `doc` 或 `documents`：读取 DOCX、检查版式、提取表格或图片；版式保真重要时渲染页面。
- `minimax-docx`：Word 结构复杂或损坏时，先修复或规范化。
- `pdf`：读取 PDF 源文件，或在无法直接抽取素材时渲染页面。
- `spreadsheets`：整理表格、CSV、XLSX 数据和可图表化材料。
- `imagegen`：用户允许生成图时，用于概念插图、背景、视觉隐喻、解释性图片；默认先写提示词，不直接生成。
- `presentations`：仅当用户明确要求根据底稿生成 PPTX 时使用。

命名技能不可用时，使用可行的本地方法继续，并说明限制。

## 工作流

### Step 0. 定位任务

确定模式、主题、听众、展示时长、格式约束和期望产物。

默认期望产物：

- PPT 文字底稿
- 报题版或提交摘要
- 统一视觉规范
- 视觉与图像提示词
- 备答或附录底稿
- 表达边界

可选后续产物：

- 根据底稿生成 `.pptx`

### Step 1. 提取要求和素材

目标：明确展示必须满足什么，以及已有材料里有哪些证据和视觉资源。

动作：

- 提取明确要求、评分标准、时长、主题范围、交付物、禁止事项。
- 提取图、表、截图、图表、图注、来源位置和可用媒体。
- 建立统一素材索引。
- 判断素材用途：主讲、备答、生成图候选、舍弃。

有源文件且适合确定性处理时，运行 `scripts/extract_requirements_and_assets.py`。

需要读取：

- `references/output-contract.md`
- `references/selection-rules.md`

### Step 2. 拆分源材料

目标：把材料转成结构化 markdown，避免后续反复处理大文件。

动作：

- 拆分源正文。
- 分离任务要求、背景、方法、案例事实、课程概念、实验结果、结论等内容。
- 去掉页眉页脚、目录噪声和重复版式文本。

运行 `scripts/chunk_sources_and_seed_draft.py` 创建内容块和底稿骨架。

### Step 3. 写展示逻辑

目标：先决定怎么讲，再决定每页写什么。

`thesis` 模式回答：

- 论文解决了什么问题？
- 前人方法卡在哪里？
- 当前方法的真实改进是什么？
- 核心贡献如何用普通话讲清楚？
- 哪些实验必须进主讲？
- 哪些内容放进备答？
- 老师最可能追问什么？

`course` 模式回答：

- 作业到底要求展示什么？
- 哪些评分点必须显性覆盖？
- 展示要捍卫什么观点、解释或方案？
- 哪些课程材料、概念、案例事实能支撑观点？
- 哪些内容很占时间但对成绩帮助不大？
- 老师或同学最可能追问什么？

`generic` 模式回答：

- 听众离场时应该理解、相信或采取什么行动？
- 哪些证据是最低可信度要求？
- 哪些细节适合放进备答或讲稿？

### Step 4. 出逐页大纲

每页必须说明：

- 页码
- 类型：主讲或备答
- 页面标题
- 页面任务
- 核心信息
- 支撑素材
- 对应要求
- 预计讲时

每页只承担一个任务。信息过载时拆页。

### Step 5. 写逐页文案和讲稿

每页必须写清：

- 页面标题
- 页面副标题，必要时填写
- 上屏内容
- 页面结论
- 页面可见文案
- 一句话核心结论
- 2 到 4 个支撑点
- 展示建议
- 推荐呈现方式，适用于时间线、矩阵、流程图、对照图、模块图等页面
- 图表或视觉放置建议
- 版式建议
- 讲解重点
- 转场句
- 预计讲时
- 追问风险

页面可见文案保持短，讲稿可以比页面文字更完整。不要把长段原文直接搬到页面。

### Step 6. 写视觉计划和图像提示词

先写统一视觉规范，再写逐页视觉计划，最后决定是否生成图。

统一视觉规范必须说明：

- 主题风格，例如学术汇报、科技治理、商业咨询、课程案例、研究答辩
- 主色、辅色、强调色和禁用色
- 标题、正文、英文术语、数字和图表标注的字体建议
- 页面结构，例如标题区、内容区、结论区、页脚信息
- 元素使用规则，例如结论条、图标、分隔线、卡片、箭头、时间线

视觉计划必须说明：

- 哪些页面使用原始证据素材
- 哪些页面需要重绘图表、流程图或结构图
- 哪些页面适合生成概念图
- 生成图的用途、画幅比例、风格、负面约束和文字安全区

用户允许时，再用 `imagegen` 或可用的 OpenAI image-2 生成图。生成图只作为视觉素材，不能作为事实证据。

### Step 7. 生成最终 PPT 文字底稿

生成 `07-final-draft/pre-draft.md`，内容应足够详细，使用户可以把它交给任意 PPT 生成平台或人工设计师继续制作。

底稿必须包含：

- 报题版：题目、100-150 字简介或课程提交摘要
- Presentation 统一视觉规范
- 标题、主题、模式、时长、听众、输出假设
- 展示目标和任务要求摘要
- 叙事主线
- 页数预算和讲述节奏
- 主讲页逐页底稿
- 备答页逐页底稿
- 图表素材清单
- 图像生成提示词
- Presentation 制作与展示建议
- 表达边界
- 给 PPT 生成平台的制作指令
- 需要用户确认的缺口

### Optional Step 8. 根据底稿生成 PPT

只有用户明确要求时执行。

输入：

- `07-final-draft/pre-draft.md`
- 素材索引
- 视觉计划
- 已生成图片或已抽取素材
- 用户指定模板或平台限制

调用 `presentations` 装配可编辑 PPTX，渲染并检查页面，再修补影响讲述的页面。

## 质量标准

合格底稿必须满足：

- 主线能在给定时长内讲完。
- 每页只有一个明确任务。
- 页面可见文案短，讲稿能自然讲出口。
- 证据素材能直接支撑口头观点。
- 课程要求、评分标准或答辩标准被显性覆盖。
- 主讲、备答、附录分离清楚。
- 统一视觉规范能约束整套 PPT 风格。
- 逐页字段足够支撑人工排版或 PPT 生成平台制作。
- 涉及争议事实或专业判断时，表达边界清楚。
- 生成图只用于增强理解或视觉清晰度。
- 任意 PPT 生成平台拿到底稿后，能理解每页该生成什么内容。

## 暂停条件

遇到以下情况才暂停询问：

- 没有源文件，也没有任务 prompt。
- 展示时长未知，并且页数预算会明显改变。
- 多个源文件版本互相冲突。
- 用户提到必须使用模板、rubric 或课程要求，但文件缺失。
- 图表无法可靠抽取，且取舍会影响主线。
- 用户要求生成图，但没有确认允许使用生成图。
